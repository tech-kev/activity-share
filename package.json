{
  "name": "activity-share",
  "version": "0.1.0",
  "private": true,
  "description": "Selbst gehostete Web-App für GPX-basierte Aktivitäts-Grafiken im Adidas-Running-Stil",
  "license": "GPL-3.0",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -k -n \"FE,BE\" -c \"cyan,magenta\" \"npm:dev:frontend\" \"npm:dev:backend\"",
    "dev:frontend": "npm --workspace frontend run dev",
    "dev:backend": "npm --workspace backend run dev",
    "build": "npm --workspace frontend run build && npm --workspace backend run build",
    "build:frontend": "npm --workspace frontend run build",
    "build:backend": "npm --workspace backend run build",
    "start": "npm --workspace backend run start",
    "typecheck": "npm --workspace frontend run typecheck && npm --workspace backend run typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
